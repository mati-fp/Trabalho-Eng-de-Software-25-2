services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres-cipcei
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: cipcei_db
      TZ: America/Sao_Paulo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - cipcei-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d cipcei_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Serviço da aplicação NestJS
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend-cipcei
    restart: always
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
      - "9229:9229" # Porta para debug (opcional)
    volumes:
      # Volume bind para hot-reload - mapeia código local para o container
      - .:/app
      - /app/node_modules # Evita sobrescrever node_modules do container
    env_file:
      - .env
    environment:
      # Sobrescreve DATABASE_HOST para usar o nome do serviço Docker
      DATABASE_HOST: postgres
      TZ: America/Sao_Paulo
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - cipcei-network
    stdin_open: true
    tty: true

  # Serviço da aplicação Next.js Frontend (standalone)
  frontend:
    build:
      context: ../cipcei-frontend-app
      dockerfile: Dockerfile
      target: deps # Usa o stage deps que tem as dependências instaladas para desenvolvimento
    container_name: frontend-cipcei
    restart: always
    ports:
      - "3001:3001"
    volumes:
      # Volume bind para hot-reload - mapeia código local para o container
      - ../cipcei-frontend-app:/app
      - /app/node_modules # Evita sobrescrever node_modules do container
      - /app/.next # Evita sobrescrever .next do container
    environment:
      # URL da API backend (acessível do navegador do usuário)
      NEXT_PUBLIC_API_URL: http://localhost:3000
      # Ambiente
      NODE_ENV: development # development para hot reload
      PORT: 3001
    command: pnpm dev # Usa o script dev para hot reload
    depends_on:
      - app
    networks:
      - cipcei-network
    stdin_open: true
    tty: true

volumes:
  postgres_data:

networks:
  cipcei-network:
    driver: bridge